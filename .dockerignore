# 让 Docker 构建上下文保持精简 —— 不把本地构建产物 / 依赖 / 临时文件发进守护进程。

# 依赖与构建缓存(镜像内 npm ci 重新生成)
**/node_modules
**/.next
**/out
**/test-results
**/playwright-report

# 临时 / 调试 / 日志
**/tmp
*.log

# 版本控制与本地配置
.git
**/.git
**/.env*

# 本地工作文档(不入库,也不进镜像)
CLAUDE.md
**/AGENTS.md

# IDE / OS
.vscode
.idea
.DS_Store
Thumbs.db

# Docker 自身文件不必进上下文
Dockerfile
.dockerignore
docker-compose.yml
